{
  "build": {
    "builder": "nixpacks",
    "buildCommand": "npm install",
    "startCommand": "npm start"
  },
  "deploy": {
    "startCommand": "npm start",
    "predeployCommand": "node -e \"require('pg').defaults.ssl = { rejectUnauthorized: false }; require('child_process').execSync('npx ace migration:run --force', { stdio: 'inherit' })\""
  },
  "variables": {
    "NODE_ENV": "production",
    "DB_CONNECTION": "pg",
    "PGSSLMODE": "no-verify"
  }
}
