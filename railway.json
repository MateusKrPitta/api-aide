{
  "build": {
    "nixpacks": {
      "buildCommand": "npm install",
      "startCommand": "npm start"
    }
  },
  "deploy": {
    "startCommand": "npm run migrate && npm run seed && npm start"
  }
}
