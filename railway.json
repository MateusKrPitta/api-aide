{
  "build": {
    "builder": "nixpacks",
    "buildCommand": "npm install",
    "startCommand": "npm start"
  },
  "deploy": {
    "startCommand": "npm start",
    "preDeployCommand": "npm run migrate && npm run seed"
  }
}
